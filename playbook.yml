---
- hosts: localhost
  pre_tasks:
  - name: Update local apt cache
    become: true
    apt:
    update_cache: true
    cache_valid_time: 3600
  tasks: 
    - include: tasks/dconf.yml
      vars:
        num_workspaces: 6
